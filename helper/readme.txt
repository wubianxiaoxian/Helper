这是一个封装的第三库，可以用来把一个秒字符串 转化为真正的本地时间，根据字符串内容的多少在固定宽度下计算出label实际的行高，获取当前设备版本，获取当前设备屏幕的大小，获得当前系统时间到指定时间的时间差字符串,传入目标时间字符串和格式，获取缓存文件在 沙盒 Library/Caches 中的路径，查看缓存文件是否超时。
注意：使用的时候，直接导入头文件即可，需要注意的时，需要同时导入nsstring-hashing文件下的库文件到工程中。 